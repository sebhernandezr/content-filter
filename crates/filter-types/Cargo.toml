[package]
name = "filter-types"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Wire types shared by the content-filter system extension and the container app"

# Deliberately dependency-light: no objc2, no tauri. Serde is here only because these types cross
# the Tauri command boundary and generate_handler! requires Serialize on command return types.
[dependencies]
serde = { workspace = true }
